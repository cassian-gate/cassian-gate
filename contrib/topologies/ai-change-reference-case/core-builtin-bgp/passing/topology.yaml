# =============================================================================
# AI-Change Reference Case — CORE variant (built-in invariant) — PASSING
#
# Two regions of distinct authorship, kept visible (REQ-4_9-2 / LD-B):
#   [AI-GENERATED IMPLEMENTATION]  the nodes + BGP config — the candidate the
#                                  gate validates; ordinary input, no special
#                                  treatment (Doctrine §1.12 / REQ-4_9-5).
#   [HUMAN-AUTHORED INTENT]        the `tests:` block — a BUILT-IN invariant
#                                  (bgp_session_up). BYTE-IDENTICAL to
#                                  failing/topology.yaml.
#
# Verdict: PASS — the AI implementation is correct.
# Evidence is lab-generated (bgp_session_up observes live state); commit it.
# =============================================================================

name: ai-change-core-bgp-passing

# ----- [AI-GENERATED IMPLEMENTATION]  eBGP peering r1 <-> r2 -----------------
nodes:
  - name: r1
    type: frr
    asn: 65001
    router_id: 1.1.1.1
    bgp:
      neighbors:
        - peer: r2
          remote_as: 65002          # correct: r2's real ASN is 65002

  - name: r2
    type: frr
    asn: 65002
    router_id: 2.2.2.2
    bgp:
      neighbors:
        - peer: r1
          remote_as: 65001

links:
  - endpoints: ["r1:eth1", "r2:eth1"]
    ipv4: ["10.0.0.1/30", "10.0.0.2/30"]

# ----- [HUMAN-AUTHORED INTENT]  built-in invariant; identical in both --------
tests:
  - name: r1_bgp_session_to_r2_up
    kind: invariant
    type: bgp_session_up
    node: r1
    dst: 10.0.0.2
    expect: pass
