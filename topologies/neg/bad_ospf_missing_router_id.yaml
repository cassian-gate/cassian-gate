# Phase 1a / Handover 4 of 8 — schema-negative fixture for REQ-H4-15 /
# R-OSPF-13c: declares 'ospf:' on a node that lacks top-level
# 'router_id'. Per LD-3 ruling, OSPF requires a router-ID; reuse the
# node's top-level 'router_id' field.
# Expected: cassian validate hard-fails at Resolve with the §6.6.2
# templated message naming the missing top-level 'router_id'.
name: bad-ospf-missing-router-id
nodes:
  - name: r1
    type: frr
    ospf:
      area: 0
      networks:
        - 10.0.12.0/24
links: []
tests: []
